# DYDATA-81 设计验收记录

> 日期：2026-08-28
> 参考：用户提供的现有结算中心截图
> 实现：`apps/web/src/components/Shell.tsx`、`apps/web/src/App.tsx`

## 对照结论

| 检查项 | 结果 | 证据 |
|---|---|---|
| 财务一级按钮位置 | 通过 | 949×466 实际视口截图中，“财务”占用用户红框所示位置，紧邻且位于“后台”上方；两项 bounding box 均完整位于视口内 |
| 财务页面归属 | 通过 | `/finance/promotion` 中只有“财务”一级项激活，六个既有财务二级入口保持原顺序 |
| 后台互斥状态 | 通过 | 切换 `/admin` 后只有“后台”激活 |
| 移动端顺序与触控 | 通过 | 390×844 与 768×1024 中“财务”位于“后台”之前，底部项无重叠；既有 44px 触控门禁继续通过 |
| 门店入口收口 | 通过 | 门店账号无“财务”与“SAP 建议”，直达 `/finance/stores` 显示现有无权限页 |
| 视觉系统一致性 | 通过 | 未新增颜色、间距、圆角、阴影、图片或图标依赖；复用 `rail-nav__item`、`mobile-bottom-nav`、财务二级导航和 `SolarIcon.details` |

## 可视检查

- 自动化生成 390×844、768×1024、1440×900 与参考同尺寸 949×466 截图。
- 949×466 对照确认：低高度桌面规则隐藏说明行和品牌归属尾标、压缩侧栏间距；左栏层级、激活色、财务/后台相邻关系与用户标注目标一致，后台与建议按钮均可达。
- 390×844 对照确认：顶部财务二级导航保持可横向浏览，底部“财务”激活且位于“后台”之前，内容区无横向溢出。
- 应用内浏览器可正常打开本地构建；因没有使用或传输用户凭据，浏览器停留在真实登录门禁，登录后的页面视觉由仓库既有浏览器 fixture 完成验证。

## 剩余偏差

- 用户截图中的金额为空态，自动化视觉 fixture 使用确定性财务数据；本轮只验证导航位置与既有页面骨架，不改变数据状态。
- 本轮未修改页面主体，因此不重新裁决财务指标卡、表格或导入区的业务视觉。
