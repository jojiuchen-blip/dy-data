# 开发日志 — 2026-09-07

> 主题：DYDATA-87 持久重算并发修复验证
> 操作人：jojiuchen-blip
> 关联计划：docs/plans/delivery-plans/sub-delivery-plan-dy-data-T5.7-system-uat.md

---

## 一、执行概要

| # | 任务 | 关联 | 状态 |
|---|------|------|------|
| 1 | DYDATA-87 持久重算并发修复验证 | T5.7 / DYDATA-87 | 进行中，生产验收待完成 |
| 2 | DYDATA-87 发布前验证收口 | 补充更新 | ✅ |
| 3 | DYDATA-87 浏览器发布回归对齐 | 补充更新 | ✅ |
| 4 | DYDATA-87 全量失败项复测 | 补充更新 | ✅ |
| 5 | DYDATA-87 订单账号到门店映射修复 | T5.7 / DYDATA-87 | 本地回归中，未部署 |

**本日关键结论**：并发修复 `7eb0904` 已由 GitHub run `34076845161` 部署，遗留重算已成功但发布为空。只读定位后正在修复订单 UID 与绑定账号 ID 不一致导致的销售门店漏配；本次映射修复尚未部署。此前任务协议漂移继续由 S4-FCR-011 承接。

---

## 二、操作详情

### 任务 1：DYDATA-87 持久重算并发修复验证
- **目标**：恢复规则发布后的重算与门店榜单、单店分账闭环；不改变订单归属分佣口径
- **操作**：恢复生产登录核验；补充真实 PostgreSQL 并发故障注入；修复心跳元数据覆盖、锁等待后过期提交、发布事实对账与旧重试合并；刷新治理索引并完成独立审查
- **结果**：真实 PostgreSQL 9 passed；元数据、续租、发布、完成和失败过期场景修复前均复现失败；SQLite 发布对账与旧重试合并4 passed；Web build及套包122项通过。完整pytest和受影响模块回归、第二次独立复审仍进行中。尚未部署，不宣称榜单恢复。
- **涉及文件**：apps/worker/settlement_rebuild.py、apps/worker/projection_publish.py、apps/worker/queued_jobs.py、tests/test_settlement_rebuild_postgres.py、tests/test_worker_collection_pipeline.py、.github/workflows/tencent-lighthouse-deploy.yml

<!-- 复杂决策型任务可展开分析：
### 任务 N：标题（决策类）
- **背景问题**：为什么要做这个决策
- **方案对比**：（表格或列表）
- **最终决策**：选了什么 + 为什么
- **涉及文件**：列表
-->

---

## 三、变更总览

### 文件变更清单

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `apps/worker/settlement_rebuild.py` | 持久认领、续租、提交围栏、发布对账及重试合并 |
| 修改 | `apps/worker/projection_publish.py` | 发布锁等待后重新校验数据库租约 |
| 修改 | `apps/worker/queued_jobs.py` | 消费持久任务和恢复失联任务 |
| 新建 | `tests/test_settlement_rebuild_postgres.py` | 真实 PostgreSQL 并发故障注入 |
| 修改 | `tests/test_worker_collection_pipeline.py` | 发布、重试和提交边界回归 |
| 修改 | `.github/workflows/tencent-lighthouse-deploy.yml` | 发布前纳入真实数据库并发门禁 |

> 收口时由 AI 从各任务「涉及文件」聚合去重生成。操作类型：新建 / 修改 / 删除。

### Git 提交记录

| 时间 | Commit | 内容 |
|------|--------|------|

---

## 四、发现的问题 / 缺陷

发现并已在本地修复：心跳覆盖发布标记、锁等待后使用过期租约、旧执行者覆盖新认领、活动指针前移导致误重算、旧重试未合并，以及旧代际清理在租约校验后等待写锁。生产重算恢复尚未验证。

---

## 五、复盘

### 做得好的
- 用真实 PostgreSQL 两会话和数据库锁状态复现竞态，再用相同测试验证修复。

### 遇到的问题
- **现象**：本地第一次受影响组合回归中两项调度测试失败；超短租约测试也出现一次调度敏感失败。
- **根因**：调度测试继承了本机商品同步配置，额外创建商品任务；0.6 秒租约用例单独复测通过，但重负载组合中失去租约，需要使用足够调度余量并直接断言持久续租事实。
- **经验**：清理测试环境中的外部业务配置；时序测试使用数据库锁条件和续租事实，不依赖亚秒级执行速度。
- **🔧 是否提炼为规则**：仅记录，沿用已有测试和数据正确性门禁。

### 今日经验总结
1. 全量重算的执行权必须持久化，成功状态应以已发布事实为依据。→ 仅记录，契约由 S4-FCR-011 承接。
2. 代码审查通过、自动化测试通过与生产业务验收是独立结论。→ 仅记录，沿用现有规则。

---

## 五·附、方法论沉淀（可选）

> 当天工作中如果有可复用的方法论、设计原则、或跨项目通用的经验，在此抽象记录。
> 普通开发日不需要填写此章节。

---

## 六、待跟进事项

- [ ] 完成回归与独立复审后提交发布；上线观察失联任务恢复及投影发布，再核对2026-08榜单和单店样本。
---

## 补充更新 1（10:17 · 窗口 1）

### 任务 2：DYDATA-87 发布前验证收口
- **目标**：记录修复的真实数据库、模块回归和审查证据，进入受控发布
- **操作**：修复旧代际清理锁顺序与有界等待；清理本机业务环境配置后重跑；使用跨两个续租周期的数据库事实断言；完成独立复审
- **结果**：受影响6文件225 passed；最终真实PostgreSQL10 passed；补充提交边界7 passed；PostgreSQL迁移与事务锁release gate通过，head=20260903_0050；独立复审Critical/Important/Minor均0。Web build及治理122项已通过。完整本地pytest仍运行，生产未部署。
- **涉及文件**：apps/worker/settlement_rebuild.py、tests/test_settlement_rebuild_postgres.py、docs/plans/execution-plan.md
---

## 补充更新 2（10:29 · 窗口 2）

### 任务 3：DYDATA-87 浏览器发布回归对齐
- **目标**：验证原子入队后的浏览器网络重试与真实API发布导入
- **操作**：本地全量发现旧断言仍要求前端额外发起重算；单项复现0与1不符；保留两次发布同幂等键断言，增加自动入队提示断言并禁止浏览器重复发起重算；主动取消旧CI防止错误放行
- **结果**：浏览器重试与真实FastAPI保存发布、冲突和CSV/XLSX批量导入4 passed；生产代码未再修改。首个发布流程34075886414已取消，未部署；以测试修正后的新提交重新执行完整发布门禁。
- **涉及文件**：tests/test_visual_smoke.py
---

## 补充更新 3（10:36 · 窗口 3）

### 任务 4：DYDATA-87 全量失败项复测
- **目标**：闭合本地完整回归发现的三个失败项，不把旧运行报告成全绿
- **操作**：记录完整运行2424 passed、129 skipped、3 failed；浏览器旧请求次数断言已由前次4项复测覆盖；调度轮询测试补齐新增持久队列依赖并断言每轮调用；SQLite心跳用例改为跨两个租约周期并读取真实续租事实；取消第二轮旧CI
- **结果**：Worker采集、子进程恢复、调度三文件59 passed、8环境限定skipped；测试变更独立复审无问题。生产代码仍为1d43306所验证内容。两个旧CI均已取消且未部署；新提交将重新跑GitHub完整门禁。
- **涉及文件**：tests/test_worker_collection_pipeline.py、tests/test_worker_subprocess_recovery.py

---

## 补充更新 4（13:38 · 窗口 1）

### 任务 5：DYDATA-87 订单账号到门店映射修复
- **目标**：修复双费用链路直接按异构账号 ID 查店导致的空投影，保留订单归属例外、有效绑定、范围、渠道和锁账规则。
- **操作**：生产只读确认已发布空结果；用虚构 UID 和门店数据先建立失败测试（3 failed / 7 passed），复用既有精确昵称绑定证据，加入唯一有效真实门店、账号日期及冲突校验。增量回归发现非门店 ID 的原始绑定被误判为冲突，按真实门店存在性先过滤后通过原回归。
- **结果**：核心结算首轮 63 passed；增量结算 44 passed；首轮受影响组合为 12 failed / 227 passed，12 项均为上述增量边界，修正后完整增量文件已通过。审查阶段对同一身份 active/unbound 并存、pending/reviewing、数字类型、精确 direct 解绑被其他身份掩盖、无关非门店状态冲突及失效 direct 门店被名称替代分别先复现失败再修复。最终核心结算与增量组合 **123 passed，80.18 秒**；独立最终审查 Ready，无 Critical/Important，另行复验 direct 失效及缺 store_id fallback 3/3 通过。Web build 通过，仅既有大包警告；治理套包 122 项与锁/协议/全局检查通过。旧全量进程因代码迭代已中止，不计作通过；最终全量 pytest 于 13:51 启动，JUnit 输出到忽略目录 `logs/dydata87-owner-matching-full.xml`，尚未结束。本次修复未提交部署，不能声明榜单或单店恢复。
- **涉及文件**：apps/worker/settlement.py、tests/test_data_settlement.py、docs/plans/execution-plan.md。
- **数据与风险**：生产详细统计口径以 Linear DYDATA-87 2026-09-07 评论为准；补充只读查询显示未匹配订单中 181 单关联同一完整绑定身份状态并存，但还需按真实 active 门店或精确 direct 身份过滤，不能将原统计等同于最终阻断数。唯一候选不等于确定计佣。无绑定及多店冲突继续阻断，不补造门店。状态枚举与 JSON 类型分布补充查询尚未取得新结果。单店通用错误文案丢失的独立问题尚未修改。
- **Foundation**：本任务按现有归属绑定契约修复实现，无新增 foundation 漂移。
- **下一步**：完成全量/审查与发布门禁后受控部署，仅重建一次并核对新投影、全国榜单和单店样本；未经用户验收不 Done。
- **最终全量源码指纹**：`apps/worker/settlement.py` SHA256 `EC5152886B0CFE172903B31997596E17AD2E44A40E615C35CC8F13C15654D425`；`tests/test_data_settlement.py` SHA256 `A5B256110EA856AFE8304106522F1F981CFF8E4511DDCA5221A15A85AE7CB766`。续跑时先核对源码指纹与 JUnit 完整结果；源码变化则旧结果不能作为该版本发布证据。
- **16:20 验证收口**：原全量为 2461 passed、2 failed、139 skipped，两个失败在采集调度测试中由额外产品同步路径产生。仅隔离本地配置与继承的抖音凭据、不禁用产品同步，模块 46 passed / 120.81 秒；相同源码清洁配置全量最终 **2463 passed、139 skipped、0 failed / 0 errors，3367.30 秒，exit 0**。JUnit 为 `logs/dydata87-owner-matching-clean-full.xml`，两源码指纹一致。未修改用户配置或生产逻辑来绕过失败。
- **发布交接**：本窗口负责明确暂存本任务四个文件并提交既有分支，随后运行受控部署；上线后仅重建一次，检查新任务及榜单/单店样本。发布前保留既有生产版本作为回滚基线，无 schema 迁移。技术验证通过不等于生产数据已恢复；生产与用户验收仍待完成。
