# 2026-09-24 商品范围与组织筛选

## 授权与范围

用户确认四项增量：指标看板全部商品/精诚养车商品/比亚迪本品；明细三类商品；看板考核提示；明细组织层级搜索。授权开发、同步主分支和服务器部署，并明确跳过Linear。开发在独立工作树进行。

## 实现决策

- 保留精诚养车原计算及适用名单，不改变日期、归属和比率公式。
- 快照metric_version为全部/本品附加范围后缀；精诚养车保留原版本兼容缓存，无数据库迁移。读取与run ID幂等校验同时验证范围。
- 精诚养车沿用SKU规则识别；本品为非精诚养车，NULL及未知商品纳入补集；明细保留原有商品可见性设置和门店权限。
- 组织筛选复用账号当前生效组织目录，集团独立于服务中心，区域键包含完整服务层级；候选项与查询均与账号范围取交集。
- 原细分类URL参数兼容保留；新界面仅暴露三类选择。指标看板继续按现行规则全局可见。
- 切换筛选时清除旧结果，避免旧范围数据被误读。

## 验证记录

- 新商品范围测试先因缺少参数失败，扩展后43项统计回归通过。
- 新API用例先复现范围未校验、组织选项404及分类为空；实施后商品和导出通过。
- 55项线索回归通过，涵盖全局/受限/空权限账号下候选项、列表、数量、导出及非法路径。
- 前端构建通过；完整pytest、独立审查及浏览器验收进行中。
- 当前未提交、未推送、未部署；最终状态以追加证据为准。

## Foundation漂移

新增商品范围及组织筛选契约，记录于本次foundation变更请求；API合同同步更新。


## 独立审查与修复

- Important：组织筛选误走账号权限的总部池排除分支。新增历史轮次导出对比测试先失败（空结果对一条历史记录），随后分开组织条件与授权范围，保留原有电话保护。
- Important：同层级切换组织保留旧门店。真实本地页面先复现：中心2区域与Store One同时保留、误空。切换组织时清除门店和门店搜索，待浏览器复核。
- Minor（本次暂缓）：演示数据仓库仍使用旧细分类，新的组织接口无演示分支；正式环境不使用该演示仓库。
- Minor（本次暂缓）：组织搜索先加载授权当前门店目录再过滤，当前规模适用；limit限制响应而非目录读取。
- Ruling：保留既有商品可见性设置与精诚养车适用门店分母。依据用户要求计算规则不变；若未来需本品专属名单，应另行定义，当前店均不能解释为本品独立资格名单。


## 页面及修复验证补充

- 本地真实后端（合成SQLite数据）验证：商品下拉三项、本品独立快照、下钻带productScope、线索口径提示、组织完整路径搜索、换区域清除旧门店、商品无结果空态；桌面1440及默认窄屏控件可操作。
- 浏览器实际下载的Excel回读通过：3个指标工作表，每张包含大区和区域附表，每张均标注比亚迪本品。
- 组织权限与总部池修复后55项回归通过；商品显示/旧契约54项通过；最终指标接口及账号访问32项通过；治理套件122项通过。
- 补充发现外层API说明仍固定精诚养车，先用测试复现后改为当前快照说明，原店均名单解释保留。
- 全量测试首轮仍运行，发现的旧契约断言已按新需求更新并专项复跑；不将首轮执行中的状态当作全量通过。


## 提交前门禁

- 完整pytest首轮：3107 passed、169 skipped、4 failed，耗时23分38秒。失败分别为三项旧契约断言及Windows CLI子进程编码；三项断言按新需求更新，CLI使用PYTHONUTF8=1，无无关业务修改。
- 完整失败集复跑 `pytest --lf -q`：4 passed。最终代码仍须在Linux CI完整门禁通过后合并。
- Web构建、git diff --check、计划结构/一致性/环境上下文、全局文件校验和122项治理测试通过。
- 由完整视觉测试生成的既有截图变更不属于本次交付，不纳入提交。
- 回滚目标为本次基线d36d991应用版本，无数据库迁移。
