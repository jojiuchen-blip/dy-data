# 抖音打榜看板生产接入核验

## 已确认口径

- 独立入口 `/metrics/douyin-ranking`，预留后续星评看板；集团、服务中心、大区、区域、门店五个层级。
- 数据下限北京时间2026-09-01；默认2026-09-07至当天，可选择日期。
- 订单量读取已有精诚养车 SKU 白名单，门店及明确绑定职人的全渠道订单按订单ID去重，不沿用结算渠道过滤。
- 店均分母采用**统计开始日**有效的精诚养车适用门店名单，含零销量门店。用户于本轮明确确认，期中名单更新不改变同一开始日的分母版本。
- 正式分配轮次立即计入24小时跟进率分母，包括未满24小时的轮次；同店未删除有效跟进且发生于分配后24小时内计分子。辅助“跟进率”无24小时时限。
- 自店核销率按 `(订单ID, 正式分配门店)` 责任样本计分母；关联券在分配之后成功、未撤销且于本店核销才计分子。异店核销不计，不修改结算中心。
- 组织版本按线索首次正式分配时刻固定；历史版本、线索组织pin、已发布快照均保留。

## 实现与审查

- 正式查询只接受 `business` 快照，预览只读 `synthetic`；不再回退旧结算聚合。
- 快照缓存5分钟，页面可手动刷新并在可见状态每5分钟重读；修复后的源数据会进入后续计算。
- 账号与绑定一次预处理，识别抖音状态2/105、UID及精确名称；1待确认不视为成功。保留绑定时间缺失标志，冲突不猜归属。可选ID补齐不会使旧昵称/失效关系复活。
- PostgreSQL事务锁串行化名单和快照发布；SQL语句20秒上限、期间事实每查询5万行，维度5万行、组织历史及汇总读取25万行上限，超量拒绝而非输出部分排名。
- 最高管理员入口 `/api/v1/admin/sync/douyin-ranking-configuration` 接收组织表与适用名单。初次双表、以后可单表；首版基线9月1日，此后使用服务器时间。相同内容幂等，错误整份拒绝。
- CSV/XLSX/XLSM文件各10MiB、XLSX解压100MiB、最多2万门店；表头按业务字段解析，允许乱序和额外无关列。腾讯代理现有64MiB足够；Web镜像为该入口单独允许21MiB请求体。
- 真实组织表1779行中唯一重复服务码 BYDSD155W 对应两条不同POI，名称和组织一致且均不属于适用名单。可保留非适用入驻记录；重复服务码若进入适用名单或名称/组织冲突则拒绝。1531家适用门店仍为1531个唯一服务码。

## 本地验证证据

- 归属+快照：67项通过；随后增加维度/汇总上限与名单边界回归。
- 正式/预览接口+业务缓存+快照：39项通过；上传接口另4项通过。
- 名单解析与版本底层：21项通过（后续真实名单边界继续补测）。
- 本地Docker独立数据库 `ranking_dashboard_gate_20260911_141359`：迁移至 `20260911_0058`；并发迁移锁、五级指标、全渠道订单、25小时辅助跟进、数字状态1自店核销、发布锁冲突与超时恢复通过。
- 前端TypeScript及Vite生产构建通过，735模块；保留既有大chunk提示。
- Playwright桌面1440和手机390：日期筛选、层级切换、辅助跟进率、未满24小时不阻断排名、无横向裁切及无页面异常，两项通过。手机指标卡改为纵向完整展示。
- 私有真实数据、测试数据库凭据和验收截图保存在本地忽略目录，不进入Git仓库。

## 迁移与发布边界

生产主分支原有0051/0052已被其他功能使用，新看板迁移接在 `20260910_0053` 后，以 `20260911_0054` 至 `20260911_0058` 单链新增；不改写已部署历史迁移。

本记录不表示已部署完整看板。发布前仍需最新主分支核对、完整CI、源证据v2上线后的真实归属复核及服务器部署验收。源证据接口v1已上线与看板上线是不同事实。

Foundation漂移：增加排名快照sidecar、名单上传及看板响应字段；不改源订单、线索分配或结算事实定义。当前Linear连接无DYDATA团队，以 LOCAL-RANKING-PRODUCTION-001 本地需求跟踪。

## 最新确认及真实名单验证
- 用户确认：店均订单量固定采用统计开始日精诚养车适用门店版本，同期跨更新日不改变分母。
- 用户确认：缺失集团/大区/区域单列“待补充归属”，保留已知组织与门店资格，后续更新不覆盖历史快照。
- 真实两份工作簿经内存数据库导入成功：1779入驻门店，1531适用门店，13门店有待补充层级（其中3家适用）；未改原始Excel。报告tmp/ranking-actual-roster-validation.json。
- 更新入口返回待补充门店数并显示提示；新前端构建通过（735模块，既有大包提示）。
- 源接口PR28已合并0821c3c，完整CI34608506039成功；腾讯手动部署34614778571运行中，不能宣称生产成功。完整看板仍在本地核验。

## 最后两项审查修复
- 发布分支移除 scripts/seed_local_douyin_ranking_snapshot.py（此前可能回退环境数据库并清空业务表）；原开发目录副本未动，线上从未运行此脚本。
- DouyinRankingPage新增复用TablePagination，支持超过50条时翻页/跳页；新增页面回归先RED缺下一页按钮，再桌面/手机2passed，改日期回第一页。
- 跨组织期间店均分母问题仍等待用户口径答复，未擅自改逻辑。完整看板暂不提交部署。

## 跨组织口径最终确认
用户明确：订单量和店均统一按统计开始日组织归属，资格名单同日固定；线索跟进及自店核销仍按首次正式分配归属。sales样本改用roster_version，metric版本升v5避免复用旧口径缓存。五级跨期调店测试先5项失败复现，再snapshot/business/API共43passed。此前review三项阻断均已有对应修复，等待最终复核。

## 2026-09-12 CI 契约同步
首轮完整CI34617866526：2992 passed / 167 skipped / 2 failed。两项失败是旧测试硬编码的门店页面权限和导航联合类型未包含新增A03/metrics；仅同步对应断言，权限重载、认证隔离及财务独立导航检查保留。完整CLI鉴权及前端用户契约模块本地26passed。需等待修正提交的完整CI，不把上一轮视为全部通过。

## 2026-09-12 源证据上线及名称核验补齐
源接口部署34614778571第一次因SSH Broken pipe中断，第二次成功；完整看板尚未部署。9/1—9/11固定观察点导出10类证据无冲突重复，Docker独立SQL与Python核验一致：正式轮次1612、24小时有效跟进8、跟进16、自店核销6，尚未套用组织及资格，不能作为最终排名。

归属核验发现现有解析器同时比较绑定表douyin_nickname与account_name，而v2导出只有前者。补充同算法source_account_name_key，不输出名称原文，不改业务归属逻辑；测试先缺字段失败，再源证据与归属模块65passed。旧导出仅初步解析810/4338候选订单，缺第二名称证据，不能将810作为最终订单量。全量归属核验及正式名单导入等待看板部署后执行。

## 2026-09-12 首次生产验收及查询超时修复
PR29合并ed1266c，CI34622872931为2994passed/167skipped；腾讯部署34626486157成功。正式组织1779/资格1531/缺失层级13导入成功，初版9/1生效。

9/1—9/11真实查询验收出现500；生产只读日志确认首个正式轮次SELECT触发PostgreSQL20秒statement_timeout，位置ranking_snapshots.calculate_snapshot。单日可返回，不能把部署成功当作完整验收通过。

将三类历史产品IN子查询改为关联候选轮次order_id的EXISTS，保留日期、formal、cutoff及匹配后行数上限；不更改指标口径或业务数据。生产只读诊断34655873041对照精确ORM查询：1612轮、345.816ms，订单使用order_id唯一索引。增加三源重叠去重/边界回归；DBAPIError回滚后返回安全503，超时测试先500失败、再通过。API/快照/业务共45passed。修复尚需发布后跨多日真实验收。
