# 开发日志 — 2026-09-10

> 操作人：jojiuchen-blip
> 关联计划：docs/plans/execution-plan.md；DYDATA-87

## 一、执行概要

门店映射分页修复已集成最新主线；专项与真实 PostgreSQL 门禁通过，生产发布尚未完成。

## 二、操作详情

### 范围与根因

用户已授权继续修复、部署及限定范围的数据修复。官方门店查询接口使用 page/size，而旧客户端传 cursor，采集器又依赖 next_cursor，导致仅采集默认第一页。官方只读采集共 36 页、1780 个唯一 POI，覆盖待修复的 91 个 POI、120 张券，对应缺失管理费 188720 分。真实明细仅保存在被忽略的运行目录，不提交。

### 实现

- 客户端改用 page/size；全部页面通过总数、页长度、唯一性及错误码检查后才写入。
- 不以 root_account 代替 POI 的实际门店账号；现有不同归属映射不覆盖。
- 写入使用唯一约束、ON CONFLICT DO NOTHING 和行锁，抵御并发归属变更，并沿用 JobImpact 审计。
- PostgreSQL 专项使用独立临时 schema，验证相同/不同归属两个连接真实锁等待；CI 和腾讯发布均显式执行。
- 已基于最新主线 58413c5 集成，保留已上线的 priority_daily 调度和 0053 迁移。

### 验证记录

- 首轮 8 项回归在旧实现均失败；修复后通过。审查补充的 5 项边界测试也完成先失败、后通过。
- 集成专项：96 passed、2 skipped；跳过的真实 PostgreSQL 测试不能算通过。
- Web TypeScript/Vite production build 通过；存在非阻断的大 chunk 提示。
- 初期记录：独立复查要求补齐 PostgreSQL 测试的审计关联表和真实锁等待，修改后等待 CI 实测。
- 初期记录：git diff --check 通过，全量 pytest 当时进行中；最终结果见下文。

- 中期记录：CI 34427668279 的真实 PostgreSQL、填充库、财务导入和含 POI 的账单并发门禁已通过；全量当时仍在运行。独立复审未发现剩余 Critical / Important 代码阻断项。

- 最终候选 CI [34427668279](https://github.com/jojiuchen-blip/dy-data/actions/runs/34427668279) 成功：2736 passed / 157 skipped，真实 PostgreSQL 单独验证通过，前端及四个 CI 镜像构建通过。生产服务器另构建五个隔离 tag 候选镜像，未切换服务。
- 新增局部计算验证：确认/未确认两例均通过；补映射后管理费新增、推广原始行不变且重跑幂等。此验证不替代完整发布/恢复测试。
- 只读生产复核：91 个映射仍缺失，120 券合计管理费差额 188720 分；13 券推广来源已关联真实确认。全局两条确认；发票、分配、活跃争议均为 0。当前费用及调整只涉及 8 月，没有 status=4 账单。
- 发布协调阻断：main 已推进至 93ace56，DYDATA-90 腾讯部署 34428997784 正在执行。已在 DYDATA-87/90 留下协调记录，未取消他方部署，未将旧基线候选切换生产。后续须基于最终生产版本协调合并，不可覆盖正式分配新主线。

## 三、变更总览

### 补充核验与发布整合（11:25）

- 用户再次确认推广须统计产生有效核销的销售订单，并授权继续执行。生产只读查询活动代次报告，截图的零本店核销样本有 19 张有效核销券：15 张映射到其他门店，4 张核销门店映射缺失；计费基数 296791 分，逐券 10% 四舍五入后推广 29680 分。缺失映射部分的管理费为 6370 分，纳入原 120 券修复范围。
- 全体当前正额推广结果剔除已全额冲回的历史结果后共 775 条；缺核销、无效/取消核销、核销月份不符、逐券费率四舍五入不符均为 0，原始费用合计 1201097 分。此检查不等于管理费缺失已修复。
- 历史空核销来源结果 4 条：3 条金额为 0，1 条 1220 分已由 -1220 分冲减；保留历史，不物理删除或改写真实财务确认。
- 确认 DYDATA-90 部署 34428997784 成功，生产版本 93ace56。已将包含其验收文档的 main dc4a1c1 无冲突合并，保留正式分配和优先调度实现；不切换旧 bab2612 候选镜像。
- 合并后专项 `test_shop_poi_pagination`、`test_shop_poi_postgres`、`test_shop_poi_settlement_repair`、`test_formal_allocation_runtime`、`test_clue_allocation_eligibility`、`test_order_status`：48 passed / 2 skipped，26.83 秒；两个 PG 跳过项仍需新候选 CI 验证。未完成新候选全量、部署、91 映射回填或三页面最终验收。
- 治理索引刷新发现 33 条既有文档引用问题，主要为历史线索 PRD/报告缺链；未扩大范围改写这些业务文档。本轮无新增计算契约漂移。

- aa3373a：客户端、采集器、仓储、适配测试及新增分页/真实 PG 测试。
- bab2612：真实锁等待和审计关联表测试修正；腾讯发布专项门禁及执行摘要。

## 四、发现的问题 / 缺陷

旧客户端分页参数错误；异常页可能被接受；并发归属覆盖需要数据库原子校验。上述已修复并有专项证据；本地缺真实 PostgreSQL，已由 CI 单独验证。

## 五、复盘

以官方完整分页、91 个缺失映射和并发锁等待验证替代单页成功假设。生产已升级其他采集主线，必须基于新主线集成。🔧 仅记录，未新增规则。

## 六、待跟进事项

- [x] bab2612 完整 CI 及候选镜像验证；后续新增局部计算测试单独通过。
- [ ] 协调最新生产基线、合并复测、备份和部署验证。
- [ ] 91 条映射及受影响券的受控回填、账单保护和三页面验收。

### 发布与数据边界

本记录不代表已部署。协调前只读观测生产版本为 e35787ff，91 条映射尚未由本任务写入。本次数据修复仅限原确认的 2026-08、10 个 SKU；保留 4 条其他归属冲突以及已有真实确认、发票和财务历史。发布前须通过 CI、真实 PostgreSQL、安全及备份检查；回填后必须回读并验证受影响券、两费、榜单、单店和财务，不能以采集成功替代结算验收。禁止隐式启用 9 月结算或重新运行无界全量重建。
