# 账号激活基础资料半小时刷新

- 用户明确要求跳过 Linear，直接把账号激活相关采集从两小时改为半小时。
- 基于主分支 `13a9e93d` 实现；本次调整 `shop_pois`、`aweme_bindings`、`backend_aweme_export` 的独立刷新周期。
- 上一轮最近成功完成后满 30 分钟可安排下一轮。任务标识包含小时和半小时分钟槽，避免同一小时第二轮与已有任务冲突；同槽重复调度继续幂等。
- 保留日批优先、重任务串行、资源保护、额度保护和浏览器认证要求，因此 30 分钟是调度间隔，不是最大数据延迟保证。
- 旧两小时任务记录仍参与最近成功时间计算；不需要数据库迁移。
- 调度专项测试：20 passed，覆盖 29:59 / 30:00 / 30:01 边界、旧记录兼容、同槽去重、下一槽及跨日、日批失败时独立刷新。
- 相关回归共 93 项通过：首次运行 92 passed，1 项因本机未添加 `apps/api` 模块路径而导入失败；补充进程内路径后该项单独复跑通过。覆盖调度、维表快照、请求预算、发布范围、额度暂停、资源保护、生命周期及任务控制。
- 完整后端回归未完成：本机第三方 `tests` 包遮蔽项目测试目录，通过仅当前测试进程的包路径设置解决收集问题后，运行至约 6% 持续无新输出，停止该测试进程；未宣称全量通过。不修改业务或测试导入代码。
- `git diff --check`、全局文档校验均通过。
- 本任务无 foundation 漂移。
- 当前为本地实现，尚未提交、推送或部署。
