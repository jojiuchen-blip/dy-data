# DYDATA-58 T5.2 本地可重复验收证据

日期：2026-08-31
范围：当前 `dy-data` checkout、本地 Windows、合成脱敏 fixture 和临时目录。只读参考了受保护的 `8gb-safe-sync-control-plane` worktree；未修改、reset 或 clean 该 worktree。未连接生产、未执行部署、未访问远程主机、未读取或复制业务数据库文件。

## 结论

状态：**LOCAL FUNCTIONAL GREEN / RELEASE BLOCKED**。

- 本地合成 fixture、增量聚合/shadow 对照、durable checkpoint、故障注入恢复和重复三轮 checksum 均通过。
- 日任务候选入口只接受显式 disposable loopback PostgreSQL 测试变量，并拒绝通用 `DY_DATABASE_URL`、`DATABASE_URL` 回退；本轮没有配置测试数据库，因此没有执行日任务或数据库写入。
- 本机是 Windows / 22 logical CPUs，无法提供真实 Linux cgroup、host memory、swap 或 OOM 证据；资源字段为 `null` 时不会被解释为资源门禁通过。
- 本报告只记录本轮实际本地命令，不把合成数据或本机功能检查扩展为生产可用性结论。

## 实际验证

临时目录：`%TEMP%/dydata-t52-local-20260831`。目录仅包含本轮合成数据和 JSON 报告。

### 专项测试

```text
python -m pytest -q tests/test_sync_end_to_end.py
17 passed in 3.38s
```

测试覆盖：

- 确定性 fixture、规模/manifest 和显式 overwrite；
- 不同 batch size 的增量与 shadow checksum；
- durable checkpoint、注入中断和恢复；
- 4C/8GB Linux 环境判定与严格 fail-closed；
- swap baseline、增长和 10% headroom 合同；
- 候选目标 allowlist、精确 child job 执行、失败阻断和测试数据库 URL 安全校验；
- 四个 T5.2 脚本的 `--help` 无副作用。

### CLI 与语法

以下四个入口均为 exit `0`，stdout 含 usage，stderr 为空；执行时未配置任何数据库变量：

```text
python scripts/ops/generate_sync_benchmark_fixture.py --help
python scripts/ops/run_sync_benchmark.py --help
python scripts/ops/run_8gb_acceptance.py --help
python scripts/ops/run_daily_candidate_acceptance.py --help
```

```text
python -m compileall -q scripts/ops/generate_sync_benchmark_fixture.py scripts/ops/run_sync_benchmark.py scripts/ops/run_8gb_acceptance.py scripts/ops/run_daily_candidate_acceptance.py tests/test_sync_end_to_end.py
compileall_exit=0

git diff --check
git_diff_check_exit=0
```

### 合成 fixture 与 benchmark

```text
python scripts/ops/generate_sync_benchmark_fixture.py --output-dir <temp>/fixture --days 3 --stores 4 --orders-per-day 5 --seed 58
fixture_exit=0
row_count=60
partition_count=12
input_sha256=e170c9391064e5df8702e948a077549987efa63971901ff824af252fc852af62
```

```text
python scripts/ops/run_sync_benchmark.py --fixture-dir <temp>/fixture --batch-size 7 --report <temp>/benchmark.json
benchmark_exit=0
batches=9
result_checksum=25dd6809d8f3aa50182d62f2a5b3c3b9a4e769fa6f56bdeaa2fcf743c071c2c5
shadow_checksum=25dd6809d8f3aa50182d62f2a5b3c3b9a4e769fa6f56bdeaa2fcf743c071c2c5
process_tree_rss_peak_bytes=null
host_used_peak_bytes=null
swap_used_start_bytes=null
swap_used_peak_bytes=null
swap_total_bytes=null
cgroup_current_peak_bytes=null
```

故障注入命令预期返回 exit `1`，并留下可恢复 checkpoint：

```text
python scripts/ops/run_sync_benchmark.py --fixture-dir <temp>/fixture --batch-size 7 --checkpoint <temp>/checkpoint.json --crash-after-batches 2
crash_exit=1
checkpoint_exists=true
completed_batches=2
next_row=14
```

去掉故障注入后复用同一 checkpoint：

```text
python scripts/ops/run_sync_benchmark.py --fixture-dir <temp>/fixture --batch-size 7 --checkpoint <temp>/checkpoint.json --report <temp>/resumed.json
resume_exit=0
resumed=true
batches=9
result_checksum=25dd6809d8f3aa50182d62f2a5b3c3b9a4e769fa6f56bdeaa2fcf743c071c2c5
shadow_checksum=25dd6809d8f3aa50182d62f2a5b3c3b9a4e769fa6f56bdeaa2fcf743c071c2c5
checkpoint_exists_after_resume=false
```

### 4C/8GB acceptance

严格命令在本地 Windows 上 fail-closed：

```text
python scripts/ops/run_8gb_acceptance.py --fixture-dir <temp>/fixture --runs 3 --batch-size 7 --report <temp>/acceptance-strict.json
acceptance_strict_exit=3
status=UNVERIFIED
release_blocker=true
environment=Windows / 22 CPUs / total_memory_bytes=0
environment_reasons=linux_required,cpu_count_must_be_4,host_memory_must_be_8gb
runs=[]
```

明确使用 `--permit-unverified-environment` 只做本地功能检查，仍为 release blocker：

```text
python scripts/ops/run_8gb_acceptance.py --fixture-dir <temp>/fixture --runs 3 --batch-size 7 --permit-unverified-environment --report <temp>/acceptance-permit.json
acceptance_permit_exit=3
status=UNVERIFIED
release_blocker=true
three_runs=true
checksum_stable=true
shadow_equivalent=true
worker_peak_at_most_2gb=false
host_peak_at_most_6_4gb=false
swap_not_growing=false
swap_headroom_at_least_10_percent=false
```

本机不提供 Linux `/proc`/cgroup 资源采样，因此四项资源检查为 `false`，不是资源安全结论。

### 日任务候选入口安全冒烟

只设置通用 `DATABASE_URL` 为伪造生产域名，未设置专用测试变量：

```text
python scripts/ops/run_daily_candidate_acceptance.py --start 2026-08-01 --end 2026-08-02 --targets clue_center --max-children 1 --report <temp>/candidate-missing-db.json
candidate_missing_test_db_exit=1
stderr=candidate acceptance execution error: RuntimeError
candidate_report_created=false
```

只设置非 loopback 的显式 T5.2 测试变量：

```text
candidate_unsafe_test_db_exit=1
stderr=candidate acceptance execution error: RuntimeError
candidate_report_created=false
```

错误输出不回显 URL 或凭据。只有通过显式测试变量、loopback host、专用本地端口和非生产角色/库名校验后，CLI 才会创建 session factory。

## 发布门禁矩阵

| 门禁 | 本轮证据 | 发布结论 |
|---|---|---|
| 本地合成 fixture / shadow / checkpoint | `17 passed`；checksum 一致；恢复后 checkpoint 删除 | 本地功能通过，不替代生产规模 |
| 真实 PostgreSQL | 未配置显式测试 PostgreSQL URL，未运行 | **UNVERIFIED / RELEASE BLOCKED** |
| Linux 4C/8GB 连续三轮 | 当前为 Windows / 22 CPUs，严格命令 exit `3` | **UNVERIFIED / RELEASE BLOCKED** |
| worker RSS、host used、cgroup | 本机采样字段为 `null` | **UNVERIFIED / RELEASE BLOCKED** |
| swap 增长、swap headroom、OOM event | 未运行 Linux 资源环境；无 OOM 证据 | **UNVERIFIED / RELEASE BLOCKED** |
| HTTPS / API / PostgreSQL 响应 | 未进行网络或服务探测 | **UNVERIFIED / RELEASE BLOCKED** |
| SSH 响应、远程测试服务器 | 未连接远程主机 | **UNVERIFIED / RELEASE BLOCKED** |
| 生产部署、生产资源门禁、canary、回滚 | 未执行 | **未验证，不得放行** |

## 阻断项

1. 尚未有真实 PostgreSQL 测试数据库证据；本轮日任务候选 runner 按安全默认拒绝执行。
2. 尚未有 Linux 4C/8GB 三轮资源证据，包含 worker RSS、host used、cgroup、swap/OOM。
3. 尚未有压测期间 HTTPS、SSH、API 和 PostgreSQL 可响应证据。
4. 本地结果不构成生产资源门禁、生产部署授权或生产可用性证明。

## 变更范围

- `scripts/ops/generate_sync_benchmark_fixture.py`
- `scripts/ops/run_sync_benchmark.py`
- `scripts/ops/run_8gb_acceptance.py`
- `scripts/ops/run_daily_candidate_acceptance.py`
- `tests/test_sync_end_to_end.py`
- `docs/reports/dydata-58/T5.2-local.md`

`scripts/ops/certify_legacy_projection_root.py` 保留未修改；`docs/runbook.md` 未修改。
