# DYDATA-96 分级内存保护与自动恢复

> **版本**：v1
> **发布日期**：2026-09-12
> **适用范围**：DYDATA-96
> **开发模式**：隔离工作区并行，主任务集成

## 0. 本计划使用指南

用户2026-09-13已确认分级策略并授权开发；需求权威为DYDATA-96。上游收集canProceed=true。

### 0.1 PRD 加载约束

读取 docs/prd/mainprd-dy-data.md 及基础索引，扩展既有同步资源保护与状态展示。

### 0.2 读前门禁 / AI 自检清单

基于已发布3d5ac35a及其验收文档。保留业务规则、租约和硬上限。

### 0.3 完成前验证门禁

残留swap、连续压力、恢复滞回、优先级、重试次数、后台提示回归。

## 环境依赖声明

| 依赖项 | 版本要求 | 检测命令 |
|---|---|---|
| Python | >=3.12 | `python --version` |
| Node | >=22 | `node --version` |

## 1. 差距基线

可用5.3GB仍因553MB swap占用DRAIN，日批未执行。

## 2. 分工与边界

策略模块与调度/UI按隔离工作区分工，主任务集成验收。不改登录、财务、分配规则。

## 3. 执行阶段

### Phase 0：内存保护与同步恢复

Entry Criteria：DYDATA-96授权且策略已确认。Exit Criteria：针对性测试及运行验证通过。

| Task | 子开发计划 | 状态 |
|---|---|---|
| T0.1 | [T0.1](sub-delivery-plan-dydata-96-memory-T0.1.md) | 进行中 |

## 4. 任务看板

[任务看板](task-kanban-dydata-96-memory.md)

## 5. 发布闸门

相关测试、Web构建及保护恢复场景通过后才可发布；本轮尚未部署。

## 6. 风险与依赖

阈值待生产校准，保留硬内存保护、单重任务及租约；不改变财务、分配和API配额。

## 7. 风险与应对

采样缺失不放行，恢复采用连续样本；硬RSS上限保留。修改计数兼容历史审计。

## 8. AI 执行示例

先用可控时钟复现swap误停，再实现策略与安全点，运行回归并核对后台合同。

## 9. PRD → 任务反向索引

| PRD | Task |
|---|---|
| docs/prd/mainprd-dy-data.md §全局设计规则 | T0.1 |
