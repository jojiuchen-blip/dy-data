# 指标排行导出开发计划

> 版本：v1；用户2026-09-14确认方向，授权直接开发部署并跳过Linear。执行主体：当前Codex任务，独立工作目录。

## 0. 本计划使用指南

本轮只扩展现有看板排序及导出；需求以已确认对话与[开发记录](../../devlog/20260914_ranking_export.md)为准。

### 环境依赖声明

| 项目 | 版本要求 | 检测命令 |
|---|---|---|
| Node | >=22 | `node --version` |

Python使用本机已配置的3.12运行时和现有测试依赖；读取、导出均复用已有openpyxl。

## 1. 差距基线

看板固定店均排行，无分指标分层级Excel导出。

## 2. 分工与边界

当前任务单独实现与验证，无schema迁移，不改计算业务口径。

## 3. 执行阶段

### Phase 0：排序与导出

Entry Criteria：用户确认方案与实施。Exit Criteria：测试、构建、CI、部署、线上导出验证通过。

| Task | 子开发计划 | 状态 |
|---|---|---|
| T0.1 | [T0.1](sub-delivery-plan-ranking-export-T0.1.md) | 进行中 |

## 4. 任务看板

[任务看板](task-kanban-ranking-export.md)

## 5. 发布闸门

本地及CI验证通过，按main发布服务器；本轮尚未部署。

## 6. 风险与依赖

导出继承权限、同一快照、防止漏页；保留数据质量说明。

## 7. 风险与应对

测试覆盖空数据、异常日期、并列、缺样本、大于200行、公式样文本。失败停止发布。

## 8. AI执行示例

先复现导出404，完成服务/页面，检查实际xlsx内容和浏览器下载，再发布验收。

## 9. PRD → 任务反向索引

| PRD | Task |
|---|---|
| docs/prd/mainprd-dy-data.md §全局设计规则 | T0.1 |

> **版本**：v1
> **发布日期**：2026-09-14
> **适用范围**：指标看板排序及导出
> **开发模式**：单任务独立工作目录

### PRD 加载约束

按子计划双链读取既有mainprd及API合同；本轮需求增量由用户对话确认。

### 读前门禁 / AI 自检清单

确认主分支、用户授权、验收口径、权限范围、依赖环境，运行计划一致性校验。

### 完成前验证门禁

专项回归、全量pytest、前端构建、治理校验、git diff --check、浏览器与生产导出验收。
