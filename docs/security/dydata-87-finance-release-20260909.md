# DYDATA-87 增量安全扫描报告

## 1. 扫描范围

- 扫描模式：partial，本次发布增量，不替代系统全量安全评估。
- 覆盖域：相对生产主线的代码、敏感信息、依赖、输入校验及门店权限；候选版本 `2e2b95f`。
- 未覆盖域：云基础设施渗透、历史模块全面审计及部署后运行验证。

## 2. 输入证据

- 执行计划：`docs/plans/execution-plan.md` DYDATA-87、T5.7。
- 代码 / 配置 / 依赖变更：仅查询、展示、测试及文档；未改依赖、迁移、部署配置或鉴权入口。
- 测试与验收材料：167 项专项通过；整合主线后 Web build、122 项治理测试及协议检查通过；独立代码审查未发现 Critical / Important。
- 部署与暴露面信息：复用现有受鉴权管理 API 与既有生产流水线，不新增端口或公开接口。
- 最终完整回归：2497 passed、139 skipped，JUnit 0 failures / 0 errors。证据缺口：CI 和部署后验收仍在发布流程中；本报告不是部署成功证明。Python audit 使用 requirements 解析出的 63 个包，不等同于生产容器已安装包清单。

## 3. 发现项

- SEC-01：Bandit B608 命中新增财务 SQL 组装。人工复核表名来自固定 `_monthly_source_spec` 分支，条件来自受控片段，月份、产品及代次值使用绑定参数；未发现将用户输入拼接为 SQL 结构的路径。历史 SQL 告警不冒充本次已全面处理。
- SEC-02：增量高置信密钥扫描匹配数 0；无新增 Cookie、私钥或数据库凭据。Bandit 的既有测试专用字符串告警不属于新增生产密钥。
- SEC-03：npm 官方 registry 生产依赖审计 0 漏洞；pip-audit 对本次解析出的 63 个包报告 0 已知漏洞。原 npm 镜像审计端点不支持，已改用官方端点完成检查，未修改项目配置。
- SEC-04：候选查询在独立进程中以生产 READ ONLY 事务验证，全国 530 行；空门店范围 0 行；单店范围 1 行。未出账结果的账单 ID 与确认金额均为空；不生成业务事实。
- PERF-01：导出仍逐行读取 SAP 状态。当前 530 行只读样本构建耗时 1.082 秒，尚无超时证据；累计账期和更大规模仍有 N+1 风险。

## 4. 风险分级

- Critical：本次增量未发现。
- High：本次增量未发现。
- Medium：PERF-01 为非安全性能观察，需后续批量查询优化与规模验证。
- Low：保留历史静态扫描告警背景，不扩大本报告结论。

## 5. 阻断项

- 阻断编号：本次增量安全检查无新增阻断项。
- 阻断原因：不适用。
- 未满足的放行条件：完整回归、CI、备份及部署后 smoke 属于独立发布门禁，未由本报告豁免。

## 6. 放行结论

- 最终结论：`PASS`。
- 结论理由：仅针对本次增量安全范围，未发现新增高风险或权限绕过；不得据此宣称整体发布完成。

## 7. 整改建议

- 立即整改：无本次安全阻断项。
- 完工前补齐：全量回归、CI、生产备份记录与页面/API smoke。
- 完工后跟踪：DYDATA-87 执行方在累计账期扩展或大规模导出前处理 PERF-01，不承诺未经测量的规模性能。

## 8. Waiver 记录（如有）

- 无豁免。
