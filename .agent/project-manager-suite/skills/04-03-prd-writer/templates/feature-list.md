# 功能列表 - {项目名称}

<!--
  本文件是功能列表模板。{} 中的内容为占位符，需替换为实际内容。
  <!-- 注释 --> 中的内容为写作指引，完成后删除。

  ═══════════════════════════════════════════════════════════════
  数据来源
  ═══════════════════════════════════════════════════════════════

  本文件基于以下上游产物生成：
  - BRD 文件：产品背景、使用者画像
  - page-delivery：页面路由表
  - 页面代码：区块结构、交互元素
  - explainer-flow：用户流程全貌
  - explainer-b-interaction：结构化交互语义（仅 locked 条目）
  - foundation-glossary：统一术语

  ═══════════════════════════════════════════════════════════════
  写作原则
  ═══════════════════════════════════════════════════════════════

  - 只写业务逻辑，不聊实现（不写接口路径、表名、组件名）
  - 区块命名使用术语表中的统一术语
  - ASCII 图从页面代码中提取实际布局

  ═══════════════════════════════════════════════════════════════
  机器校验锚点（不得改名 / 删除 / 改表头）
  ═══════════════════════════════════════════════════════════════

  必需章节：
  - ## 产品背景与定位
  - ### 产品是什么
  - ### 使用者
  - ## 功能总表
  - ## 页面布局全景
  - ## 区块详情

  固定表头：
  - | # | 页面 | 区块 | 功能说明 | subprd文件 | 状态 |

  页面布局全景：
  - 页面小节标题固定为：### {页面名称}（路由: {/path}）
  - 每个页面小节必须恰好一个 ASCII 代码围栏
  - 带圈数字数量必须与该页面在功能总表中的区块数量一致
  - 带圈编号必须使用功能总表 # 列的全局编号（全局唯一）：第二个页面接着上一页继续编号
    （如 ④ ⑤），不得在每个页面内重新从 ① 起编，否则 prd-check 的
    feature-list.panorama-block-ids 校验会失败

  区块详情：
  - 每个区块必须包含：**所属页面**： / **布局**： / **业务逻辑**： / **涉及使用者**：
-->

> 生成时间: {YYYY-MM-DD HH:MM}
> 来源: prd-writer Phase 2
> 上游: page-delivery-{slug} + explainer-flow-{slug} + explainer-b-interaction-{slug} + docs/prd/foundation/foundation-glossary-{slug}

---

## 产品背景与定位

<!--
  从 BRD 提取，3-5 句话说清产品定位。写完后做"3 秒理解"测试：
  - 这个产品解决什么问题？
  - 用户打开后第一眼看到什么？
  - 和竞品/旧方案有什么本质区别？
-->

### 产品是什么

{产品定位描述}

### 使用者

| 使用者 | 使用场景 |
|--------|---------|
| **{使用者类型1}** | {场景描述} |
| **{使用者类型2}** | {场景描述} |

---

## 功能总表

<!--
  前置速查表，一眼看到项目全貌。
  - # 列使用全局递增编号
  - 页面名称与 page-delivery 中的路由对齐
  - 功能说明用一句话概括业务目的
  - subprd文件 / 状态列在 Phase 4 逐份回填
  - 状态固定为：待开始 / 待确认 / 已确认
  - 全部状态为已确认前，不得声明完整版 PRD 已完成
-->

| # | 页面 | 区块 | 功能说明 | subprd文件 | 状态 |
|---|------|------|---------|-------------|------|
| 1 | {页面名} | {区块名} | {一句话功能说明} | [01-subprd-{区块英文短名}.md](subprd/01-subprd-{区块英文短名}.md) | 待开始 |
| 2 | {页面名} | {区块名} | {一句话功能说明} | [02-subprd-{区块英文短名}.md](subprd/02-subprd-{区块英文短名}.md) | 待开始 |

---

## 页面布局全景

<!--
  按页面逐个画全景 ASCII 图。
  - 每个区块标编号（① ② ③），编号 = 功能总表 # 列的全局编号，与区块详情中的编号对应
  - 编号全局唯一：后续页面延续前一页继续编号（下方第二个页面示例为 ④ ⑤），不重新从 ① 开始
  - ASCII 图从页面代码中提取实际布局结构
-->

### {页面名称}（路由: {/path}）

```
┌─────────────────────────────────────┐
│           ① {区块名}                │
├─────────────────────────────────────┤
│           ② {区块名}                │
│                                     │
├─────────────────────────────────────┤
│           ③ {区块名}                │
└─────────────────────────────────────┘
```

### {另一个页面}（路由: {/path}）

```
┌─────────────────────────────────────┐
│           ④ {区块名}                │
├─────────────────────────────────────┤
│           ⑤ {区块名}                │
└─────────────────────────────────────┘
```

---

## 区块详情

<!--
  逐个展开每个区块。编号与页面全景图中的编号对应。
  每个区块包含：
  1. 所属页面
  2. ASCII 布局图（该区块内部结构）
  3. 业务逻辑（只讲做什么，不讲怎么实现）
  4. 涉及使用者
-->

### ① {区块名}

**所属页面**：{页面名称}（路由: {/path}）

**布局**：

```
┌──────────────────────────────────┐
│  {区块内部 ASCII 布局}            │
│  标注关键交互元素的位置            │
└──────────────────────────────────┘
```

**业务逻辑**：
- {业务逻辑点 1}
- {业务逻辑点 2}
- {业务逻辑点 3}

**涉及使用者**：{使用者列表}

---

### ② {区块名}

**所属页面**：{页面名称}（路由: {/path}）

**布局**：

```
┌──────────────────────────────────┐
│  {区块内部 ASCII 布局}            │
└──────────────────────────────────┘
```

**业务逻辑**：
- {业务逻辑描述}

**涉及使用者**：{使用者列表}
